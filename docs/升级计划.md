# RetailGuard Copilot 9 周升级计划

> 文档定位：**周粒度落地动作**。承接 `需求.md`（要什么）、`PRD.md`（长什么样）、`设计.md`（怎么做），把它们拆成可执行的周里程碑。
> 进度规则：每周末必须产出 ① 可运行代码 + ② 对应文档/AGENTS.md 同步 + ③ 一张数据图或截图；用户 review 通过才能进下一周（对齐全局 AGENTS.md §10）。
> 节奏溢出：每周一晚盘上周遗留，宁可砍 W8 内容也要保 W1-W7 质量。

---

## 顶层节奏（9 周）

| 周 | 主题 | 简历产出物 |
|---|---|---|
| W1 | RAG 知识库 + Knowledge Agent + tenant_id 中间件 | "端到端 RAG，混合检索+Rerank，Top1 ≥ 80%，多租户隔离" |
| W2 | Eval 体系 + Langfuse 全链路观测 | "5 维度评测 200 用例 + 全链路 trace" |
| W3 | Planner + Critic + Interrupt 人审 + PG Checkpoint | "7-Agent 协作 + 断点续跑" |
| W4 | 三层融合风控 + 动态模型路由 + 语义缓存 | "可解释风控 + 成本下降 ≥ 50%" |
| W5 | Celery 异步 + 灰度三版本做实 + A/B 对比报告 | "三代架构演进，v3 较 v1 显著提升" |
| W6 | JWT + RBAC + 多租户 + 4 角色前端 | "按钮+接口级 RBAC + 5 租户隔离 + 4 角色闭环" |
| W7 | 中规模 mock 数据 + 性能优化 + 压测脚本 | "50 QPS / P95 < 3s 实测" |
| W8 | MCP 真 stdio 重写 + 测试 + CI + 文档发布 | "Claude Desktop 接入 + 覆盖率 ≥ 70% + CI 全绿 + 博客" |
| W9 | Buffer：补刀 + 演示走查 + 简历定稿 | "12 条演示主线全绿 + 简历投递" |

---

## 17 个里程碑（粗略对照）

| # | 里程碑 | 周 |
|---|---|---|
| 1 | KB 灌库脚本就绪 | W1 |
| 2 | RAG 端到端流式带引用 | W1 |
| 3 | tenant_id 中间件全链路 | W1 |
| 4 | 200 条评测数据集就绪 | W2 |
| 5 | Langfuse trace 全覆盖 | W2 |
| 6 | CI 接 eval smoke | W2 |
| 7 | Planner+Critic 上线 | W3 |
| 8 | PG Checkpoint Interrupt resume | W3 |
| 9 | 三层风控融合可解释决策链 | W4 |
| 10 | ModelRouter + 语义缓存 | W4 |
| 11 | Celery 批量任务 + 进度 WS | W5 |
| 12 | v1/v2/v3 真功能差异化 + A/B 报告 | W5 |
| 13 | JWT+RBAC+多租户 4 角色全闭环 | W6 |
| 14 | 中规模数据灌库 + 压测脚本 | W7 |
| 15 | MCP stdio 重写 + Claude Desktop 跑通 | W8 |
| 16 | 测试覆盖率 ≥ 70% + CI 全绿 + Docker 一键拉 | W8 |
| 17 | 12 条演示主线 + 博客 + 简历定稿 | W9 |

---

## W1：RAG 知识库 + tenant_id 中间件

### 1.1 数据准备
- [ ] `scripts/generate_kb.py`：调 LLM 批量生成 1000 篇模拟 KB
  - 退换货政策 250 / 物流规则 150 / 商品 FAQ 400 / 活动 200
  - 每篇 Markdown + frontmatter（category / sku / effective_from / effective_to）
- [ ] `docs/knowledge_base/` 落地

### 1.2 检索基建
- [ ] `docker-compose.yml` 增 Qdrant 服务（端口 6333）
- [ ] 新增 `python-impl/rag/` 七个模块（按 `rag/AGENTS.md`）
- [ ] `scripts/ingest_kb.py` 一键灌库（幂等）

### 1.3 多租户基线（W1 必做，避免后期翻动业务代码）
- [ ] `python-impl/middleware/tenant.py`：从 JWT 或 header 取 tenant_id，写 ContextVar
- [ ] `python-impl/db/session.py`：SQLAlchemy event listener 自动注入 where
- [ ] `with skip_tenant_filter(reason)` 上下文管理器 + audit_log 写入
- [ ] 已存在业务表（orders/tickets 等）补 `tenant_id` 列 + 默认 tenant-a 迁移

### 1.4 Knowledge Agent
- [ ] `python-impl/agents/knowledge_agent.py`（重命名/重写 knowledge_rag.py）
- [ ] supervisor + intent_router：增 `knowledge` 分支
- [ ] `/api/v1/chat` 端到端 SSE 流式 + 引用块

### 1.5 前端
- [ ] `ChatView.vue` 渲染引用卡片（点击展开抽屉）

### 1.6 测试 & 文档
- [ ] 5 条 RAG 单测 + 5 条 tenant 隔离单测
- [ ] `rag/AGENTS.md` 同步实际文件清单
- [ ] README 顶部加 "RAG" badge

**W1 验收**：
- 顾客问 "耳机能 7 天无理由吗" → 流式答案带引用 → 点引用展开原文
- A 租户用户检索看不到 B 租户文档（payload filter）
- `pytest tests/integration/test_rag.py` 全绿

---

## W2：Eval 体系 + Observability

### 2.1 评测数据集
- [ ] `python-impl/eval/datasets/` 五份 jsonl，按 `eval/AGENTS.md` schema
- [ ] GPT-4o/Claude 批量合成 + 人工校 20% 关键样本
- [ ] PR 合入：禁直 push，强制 review

### 2.2 评测框架
- [ ] `python-impl/eval/runner.py`：加载 → 调用 → 评测 → 报告
- [ ] `judges/exact_match.py` + `judges/ragas_judge.py` + `judges/llm_judge.py`
- [ ] `report.py`：Markdown + matplotlib（折线 / 雷达 / 饼图）

### 2.3 Langfuse 接入
- [ ] `docker-compose.yml` 增 langfuse + langfuse-db
- [ ] `python-impl/tracing/observe.py` 装饰器（异步上报 + 失败仅日志）
- [ ] 现有所有 Agent 节点 + LLM 调用包 `@observe`
- [ ] trace 字段：input / output / latency / token / model / cost / version / tenant_id

### 2.4 CI 接 eval smoke
- [ ] `.github/workflows/eval.yml`：PR 触发，每数据集抽 10 条
- [ ] 结果以评论形式贴回 PR

### 2.5 文档
- [ ] README 加 4 张截图（trace tree / 延迟分布 / 成本饼图 / 错误样例）
- [ ] `eval/AGENTS.md` 同步数据集行数

**W2 验收**：
- `make eval` 跑出 `docs/eval_reports/{date}_{sha}.md`
- Langfuse UI 看完整 trace tree
- 示例 PR 自动评论 smoke 摘要

---

## W3：Planner + Critic + Interrupt 人审 + PG Checkpoint

### 3.1 Planner
- [ ] `agents/planner.py`：复杂请求 → PlanStep[] (Pydantic 强约束)
- [ ] supervisor 路由：complexity ≥ 60 / 多动作 → planner
- [ ] 失败重试 ≤ 2 次；再失败 → 兜底转人工 + 告警

### 3.2 Critic
- [ ] `agents/critic.py`：检查金额一致 / 政策合规 / 字段完整 / 与历史不矛盾
- [ ] 输出 approve / revise / escalate
- [ ] revise 回退最多 2 次

### 3.3 PG Checkpoint + Interrupt
- [ ] `docker-compose.yml` 加 postgres（如未有）
- [ ] LangGraph PostgresSaver 接入 schema `langgraph`
- [ ] thread_id 改为 `{tenant_id}:{user_id}:{conv_id}` 三段式
- [ ] `interrupt_before=["risk_review"]` + fusion 内部条件触发
- [ ] 前端 `ReviewView` 风控人审 Approve/Reject/Escalate → resume API

### 3.4 状态图导出
- [ ] `graph.get_graph().draw_mermaid()` 输出最新拓扑
- [ ] 嵌入 README 顶部

### 3.5 测试
- [ ] `tests/agent_replay/test_planner_critic.py`：复杂多步 case 回放
- [ ] `tests/integration/test_interrupt_resume.py`：人审 Approve 后 resume 状态完整

**W3 验收**：
- 输入 "退两件其中一件换地址再发" → Planner 拆 3 步 → Critic 校验 → 金额超阈值 → Interrupt → 人审 → resume → 完成

---

## W4：三层融合风控 + 动态模型路由 + 语义缓存

### 4.1 三层融合风控
- [ ] `python-impl/risk/{rules,features,llm_scorer,fusion}.py` 按 `risk/AGENTS.md`
- [ ] `risk/prompts/llm_scorer.md` 版本化 prompt
- [ ] `config/risk_weights.yaml` + Redis 热加载
- [ ] 改造 `agents/risk_review.py` 变薄壳，调 `risk.evaluate()`
- [ ] DB 表 `risk_features` / `risk_decisions` migration
- [ ] 前端 `ReviewView` 决策链树形可视化组件

### 4.2 动态模型路由
- [ ] `python-impl/llm/router.py` + `model_specs.py` + `prompt_loader.py`
- [ ] `config/model_routing.yaml`
- [ ] 现有所有 LLM 调用替换为 `call_llm(profile=...)`
- [ ] `scripts/check_no_direct_llm.py` pre-commit hook 落地

### 4.3 语义缓存
- [ ] `python-impl/llm/semantic_cache.py`（Redis VSS）
- [ ] 装饰器应用到 knowledge / intent_router / greeting
- [ ] 豁免清单覆盖率 100% 单测

### 4.4 成本面板
- [ ] `/api/v1/admin/cost` 按模型/Agent/版本/租户分组
- [ ] AdminDashboard 成本曲线 + 缓存命中节省图

### 4.5 测试
- [ ] 30 条风控 case（正常/边缘/攻击）单测
- [ ] LLM 不可用降级 case 必跑
- [ ] 缓存命中/豁免单测

**W4 验收**：
- 退款 999 元 case → 决策链树形完整展示三层证据 + 融合分
- 简单 query 走 light profile（Langfuse 验证），复杂走 heavy
- 同一 query 二次访问命中缓存（< 500ms + trace cache_hit=true）

---

## W5：Celery 异步 + 灰度三版本做实 + A/B 对比

### 5.1 Celery
- [ ] `docker-compose.yml` 加 celery worker（队列 review/ingest/eval）
- [ ] `python-impl/tasks/{celery_app,batch_jobs,progress,dead_letter}.py`
- [ ] `/api/v1/tasks/...` + `/ws/tasks/{id}`
- [ ] 前端任务进度组件（WS + 轮询 fallback）

### 5.2 灰度三版本（核心）
- [ ] `agents/versions/{v1,v2,v3}.py` 三独立 build 函数
- [ ] supervisor 入口根据 RolloutManager 选 graph
- [ ] RolloutManager.decide() 真正接入 `/api/v1/chat`
- [ ] `/api/v1/admin/rollout` GET/PUT + audit_log

### 5.3 A/B 对比报告
- [ ] eval runner 加 `--version v1,v2,v3`
- [ ] `docs/architecture_evolution.md`：对比表 + 折线图 + 工程归因分析
- [ ] README 顶部嵌演进图

### 5.4 灰度运营接口
- [ ] AdminDashboard 三版本实时指标分组
- [ ] RolloutView 滑块 + 预览生效占比 + 最近变更审计

**W5 验收**：
- 管理员把 v3 权重调到 0 → dashboard 秒级反映
- `make eval --all-versions` 输出三版本对比报告
- 客服触发 50 条批量审核 → 后台跑 → 进度条 → 完成通知

---

## W6：JWT + RBAC + 多租户 + 4 角色前端

### 6.1 JWT 鉴权
- [ ] `python-impl/auth/jwt.py`：HS256 + access 30min + refresh 7d
- [ ] `/auth/login` / `/auth/refresh` / `/auth/me` / `/auth/logout`
- [ ] Redis refresh_token 白名单
- [ ] bcrypt 密码 + 失败 5 次锁 5 分钟

### 6.2 RBAC
- [ ] DB migration：roles / permissions / user_roles / role_permissions
- [ ] `auth/permissions.py`：`require_perm` / `require_role` 依赖
- [ ] 现有所有写接口加权限装饰器
- [ ] 跨租户访问 → 404 + audit_log（防枚举）

### 6.3 多租户管理
- [ ] `/api/v1/admin/tenants` CRUD
- [ ] 5 个 mock 租户 + 各角色账户初始化（bootstrap.py）
- [ ] 前端 TenantSelector + 自动带 `X-Tenant-Id` header

### 6.4 4 角色前端整合
- [ ] 顾客视图：ChatView / OrderListView / TicketDetailView / RefundFormView
- [ ] 客服视图：AgentDashboardView / SessionInboxView / ConversationView / TicketHandleView
- [ ] 风控视图：ReviewQueueView / RiskDecisionView
- [ ] 管理员视图：AdminDashboardView / RolloutView / TenantManageView / CostView / TraceView
- [ ] 合并/废弃：UserDashboardView → 合入 ChatView；AuditView/OpsView → 重组入风控/管理员
- [ ] `v-permission` 指令落地
- [ ] axios 401 静默 refresh，refresh 失败跳 `/login?redirect_to=...`

### 6.5 测试
- [ ] `tests/integration/test_auth.py`：完整 token 生命周期
- [ ] `tests/integration/test_rbac.py`：4 角色 × 20 端点矩阵
- [ ] `tests/integration/test_tenant_isolation.py`：跨租户访问拦截 + 防枚举

**W6 验收**（按 PRD §6 走 20 条 user story 中 P0 关键）：
- 顾客菜单看不到风控/管理员
- A 租户管理员看不到 B 租户工单（包括构造 header）
- token 过期自动 refresh 无感；refresh 失败跳登录
- 4 角色登录后落对应默认页

---

## W7：中规模 mock 数据 + 性能优化 + 压测

### 7.1 数据生成
- [ ] `scripts/generate_mock_data.py`（幂等）：
  - 5 租户 + 1000 用户（每租户 200） + 10000 订单
  - 5000 工单（状态机覆盖各分支） + 含 200 条历史退款的特征基线
  - 1000 KB 文档（W1 已有则跳过）
  - 200 评测用例（W2 已有则保留）
- [ ] `faker[zh_CN]` + LLM 生成自然中文

### 7.2 性能优化
- [ ] 索引 review：`(tenant_id, status, created_at)` 复合索引上线
- [ ] N+1 排查（SQLAlchemy `joinedload` / `selectinload`）
- [ ] Redis 缓存：用户 perms / tenant config（TTL 60s）
- [ ] FastAPI worker 调优（uvicorn --workers 4）
- [ ] Qdrant HNSW 参数微调

### 7.3 压测
- [ ] `scripts/bench/locustfile.py`：模拟 4 角色行为 50 并发
- [ ] 三场景：纯 chat / chat+RAG / 复杂业务（Planner+Critic+Risk）
- [ ] 压测报告 `docs/perf_report.md`：每场景 QPS / P50/P95/P99 / 错误率 / 平均 token 成本

### 7.4 调优记录
- [ ] `docs/perf_optimization.md`：发现的瓶颈 + 改动 + 前后对比

**W7 验收**：
- 50 QPS / P95 < 3s 实测（含 RAG）
- 缓存命中率 ≥ 30%
- 错误率 < 1%

---

## W8：MCP stdio 重写 + 测试 + CI + 文档发布

### 8.1 MCP 真协议
- [ ] `python-impl/mcp/mcp_server.py` 用官方 `mcp` SDK 重写为 stdio
- [ ] tools/list 暴露 4 个：query_order / list_refunds / get_kb_doc / risk_check
- [ ] 现有内部 tool registry 保留供 Agent 内部用
- [ ] `MCP_API_TOKEN` 绑定 service account
- [ ] `docs/mcp_integration.md` + Claude Desktop 配置示例
- [ ] 录 30s GIF：Claude Desktop 调本系统查订单

### 8.2 测试与覆盖率
- [ ] `tests/unit/` 补到 ≥ 70% 覆盖（重点：risk 三层 / llm.router / 缓存豁免）
- [ ] `tests/contract/` MCP 协议契约 + OpenAPI schema 校验
- [ ] `tests/agent_replay/` 12 条演示主线 fixture 全跑
- [ ] `pytest --cov-fail-under=70` 接 CI

### 8.3 CI 与机械化校验
- [ ] `.github/workflows/ci.yml`：lint + mypy + unit + integration + build + drift-scan
- [ ] `pre-commit` 7 项 hook（见根 AGENTS.md §4）
- [ ] `scripts/check_*.py` 全部实现并接 pre-commit
- [ ] `scripts/drift_scan.py` 自动开 PR `chore: drift fix`（mock，不真 push）

### 8.4 部署与文档
- [ ] `docker-compose up -d` 一键全栈起（10 服务）+ healthcheck
- [ ] `scripts/bootstrap.py` 幂等初始化（租户/角色/权限/mock 数据）
- [ ] README 重写顶部：Highlights（10 条数据化句式）+ Architecture（4 张 Mermaid 图）+ Quickstart + Eval Report + Roadmap
- [ ] 5 个 badge（build / coverage / langgraph / docker / license）

### 8.5 内容发布
- [ ] 知乎/掘金博客《用 LangGraph + MCP 搓一个企业级售后 Copilot》草稿
  - 问题背景 → 7-Agent 拓扑 → 三层风控 → 灰度对比 → 评测体系 → 踩坑总结
- [ ] 博客链接挂 README

**W8 验收**：
- clone → `.env.example` → `docker-compose up` → 5 分钟跑通
- Claude Desktop 接 MCP 查订单成功
- CI 全绿，覆盖率 ≥ 70%
- 博客链接挂 README

---

## W9：Buffer + 演示走查 + 简历定稿

### 9.1 12 条演示主线走查
按 `PRD.md §8` 12 条剧本逐条录屏，每条 < 30s。

### 9.2 砍刀（减负）
- [ ] 删未用 WebSocket 通知中心（如不在主路径）
- [ ] 合并 `UserDashboardView.vue`（W6 未完成则补）
- [ ] 删 MySQL 适配代码（统一 PG）
- [ ] README 内死链清理

### 9.3 简历定稿
- [ ] 项目段落 10 条 highlights 选 5-6 条（每条带具体数字）
- [ ] GitHub README 优化首屏（架构图置顶 + Quickstart 5 行）
- [ ] 投递清单（5-10 家 JD 对照）

### 9.4 漂移最后扫描
- [ ] `make drift` 全绿
- [ ] AGENTS.md 与实际目录 100% 一致
- [ ] 文档变更记录补齐

**W9 验收**：
- 12 条演示主线全跑通有录屏
- 简历定稿可投递
- README 5 分钟新人能跑起来

---

## 关键文件修改地图（汇总）

| 新增 | 周 |
|---|---|
| `python-impl/rag/` 7 文件 | W1 |
| `python-impl/middleware/tenant.py` + `db/session.py` | W1 |
| `python-impl/eval/` 重写（替换 evals/） | W2 |
| `python-impl/tracing/observe.py` | W2 |
| `python-impl/agents/{planner,critic}.py` | W3 |
| `python-impl/risk/{rules,features,llm_scorer,fusion}.py` + `prompts/` | W4 |
| `python-impl/llm/{router,semantic_cache,model_specs,prompt_loader}.py` | W4 |
| `python-impl/config/{risk_weights,model_routing}.yaml` | W4 |
| `python-impl/tasks/{celery_app,batch_jobs,progress,dead_letter}.py` | W5 |
| `python-impl/agents/versions/{v1,v2,v3}.py` | W5 |
| `python-impl/auth/{jwt,permissions,user_service}.py` | W6 |
| `python-impl/exceptions/error_code.py` | W6（W1 起逐步补） |
| `scripts/{generate_kb,ingest_kb,generate_mock_data,bootstrap,drift_scan,check_*}.py` | 各周 |
| `python-impl/mcp/mcp_server.py` 重写 | W8 |

| 修改 | 周 |
|---|---|
| `agents/supervisor.py` 加 knowledge/planner/critic 路由 | W1+W3 |
| `agents/risk_review.py` 改造为薄壳 | W4 |
| `agents/intent_router.py` 加复杂度估算 | W3 |
| `api/main.py` 加 admin/auth/tasks/eval/review 端点 | W2-W8 |
| `frontend/src/views/*` 14 个角色视图整合 | W6 |
| `docker-compose.yml` 加 Qdrant/Langfuse/PG/Celery worker | W1+W2+W3+W5 |
| `README.md` 每周追加，W8 重写顶部 | 各周 |

---

## 风险与缓冲

| 风险 | 等级 | 缓解 |
|---|---|---|
| W1 多租户改造侵入面广 | 高 | 第一周就贯穿，避免后期回头改 |
| W3 Planner 输出不稳 | 中 | Pydantic schema + 强约束 prompt + 重试 ≤ 2 |
| W4 三层风控联调复杂 | 高 | 拆四步走（rules→features→llm→fusion），每层独立测 |
| W4 LLM 直调散落 | 中 | pre-commit hook 强制 + W4 首日全量替换 |
| W5 三版本回放风险 | 中 | v1/v2 重写为独立模块，不去 git 历史回滚 |
| W6 RBAC 接口遗漏 | 高 | 写 4 角色 × 20 端点矩阵单测，缺一不能通过 |
| W7 性能不达标 | 中 | 索引优化 + Redis 缓存兜底；P95 暂定 3s，实测后回填到 需求.md |
| W8 MCP 调试坑 | 中 | 参考官方示例 + Claude Desktop 调试 + 协议契约测试 |
| 9 周溢出 | 中 | 每周一晚盘上周遗留，宁可砍 W8 内容也要保 W1-W7 质量 |

---

## 验证总清单（W9 末统一过一遍 12 条）

对应 `需求.md §7` 与 `PRD.md §8`：

1. 知识问答（F-04）
2. 简单业务（F-05）+ 跨租户拒绝（F-03）
3. 复杂业务（F-08 / F-09）
4. 风控触发→决策链→resume（F-06 / F-10 / F-11）
5. 模型路由（F-14）
6. 语义缓存（F-15）
7. 异步任务（F-16）
8. 灰度对比（F-12 / F-13）
9. 多租户隔离（F-03）
10. 观测全链路（F-17）
11. 评测报告（F-18）
12. MCP 对外（F-19）

跑通 = 可作为简历主项目投递。

---

## 变更记录

| 日期 | 修改人 | 摘要 |
|---|---|---|
| 2026-05-25 | W0 brainstorm | 9 周节奏定稿。从 6 周扩到 9 周（W6 加 JWT+RBAC+多租户+4 角色前端 / W7 加中规模数据+压测 / W8 含 MCP+测试+CI+发布 / W9 buffer 与简历定稿）。集成 harness 规范、ErrorCode 枚举、tenant 中间件、机械化校验。文件位置从 `~/.claude/plans/` 迁移到 `docs/`（仓库即唯一事实来源） |
